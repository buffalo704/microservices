# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build moderation`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 4005:4005 -t moderation`.
FROM node:18-alpine

ENV HOST=0.0.0.0
ENV PORT=4003

WORKDIR /app

RUN addgroup --system moderation && \
          adduser --system -G moderation moderation

COPY dist/apps/moderation moderation
RUN chown -R moderation:moderation .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix moderation --omit=dev -f install

CMD [ "npm", "start", "moderation" ]
