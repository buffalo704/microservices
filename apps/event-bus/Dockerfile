# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build event-bus`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 4005:4005 -t event-bus`.
FROM node:18-alpine

ENV HOST=0.0.0.0
ENV PORT=4005

WORKDIR /app

RUN addgroup --system event-bus && \
          adduser --system -G event-bus event-bus

COPY dist/apps/event-bus event-bus
RUN chown -R event-bus:event-bus .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix event-bus --omit=dev -f install

CMD [ "npm","start", "event-bus" ]
